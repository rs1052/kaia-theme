# Kaia browser audit fixture

This workspace is copied into a disposable volume. `src/App.tsx` is modified and
`audit-note.txt` is untracked after the initial commit so Source Control has a
repeatable state. Open `src/diagnostics.js` while inspecting the Problems panel,
and run `node terminal-ansi.js` in the integrated terminal for ANSI samples.
